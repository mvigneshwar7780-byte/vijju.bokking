[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    concurrency: spawns real threads against real connections; slower
filterwarnings =
    error::DeprecationWarning
    ignore::DeprecationWarning:passlib.*
